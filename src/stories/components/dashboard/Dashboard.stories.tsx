import { Story } from '@storybook/react'
import { Dashboard } from '../../../components'

export const Template: Story<any> = args => <Dashboard />

export const WithLargeDescription = Template.bind({})
